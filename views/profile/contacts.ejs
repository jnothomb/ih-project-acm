<!-- MUST RENAME THIS PAGE IN ALL -->

<!-- main.html -->
<!DOCTYPE html>
<html>

<head>
    <title>Contacts</title>
</head>

<body>

    <h1>Contacts</h1>
    <br>

    <!-- link to user profile -->

    <a href="/profile/<%= user.id %>">
        <%= user.firstName + " " + user.firstName %>
    </a>
    <br>

    <!-- search function -->

    <form action="/contacts/search" method="POST">
        <input type="text" name="contactSearch">
        <button type="submit">Search</button>
    </form>

    <% if(searchResult !== null) { %>
        <pre><%= JSON.stringify(searchResult,null,2) %></pre>

        <!-- SHOULD BE HERE ?????????-->

        <h3>Search Result</h3>

        <ul>
            <% users.forEach(el => { %>
                <% if(user.username !== el.username) { %>
                    <li>
                        <a class="btn-user" href="/profile/<%= el.id %>">
                            <%= el.firstName %>
                                <%= el.lastName %>
                        </a>
                    </li>

                    <% } %>
                        <% }) %>

        </ul>

        <% } else { %>

            <h3>List</h3>

            <ul>
                <% users.forEach(el => { %>
                    <% if(user.username !== el.username) { %>
                        <li>
                            <a class="btn-user" href="/profile/<%= el.id %>">
                                <%= el.firstName %>
                                    <%= el.lastName %>
                            </a>
                        </li>

                        <% } %>
                            <% }) %>

            </ul>

            <% } %>



</body>

</html>